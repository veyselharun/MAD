# Code analysis options.
# Determine how strict Flutter should be when analyzing your code. 
# You're telling the analyzer to take it easy. You can always tune this later. 
# In fact, as you get closer to publishing an actual production app, you will 
# almost certainly want to make the analyzer stricter than this.

include: package:flutter_lints/flutter.yaml

linter:
  rules:
    avoid_print: false
    prefer_const_constructors_in_immutables: false
    prefer_const_constructors: false
    prefer_const_literals_to_create_immutables: false
    prefer_final_fields: false
    unnecessary_breaks: true
    use_key_in_widget_constructors: false